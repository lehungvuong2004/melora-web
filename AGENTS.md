# AI EXECUTION MODE

You are an autonomous senior full-stack engineer.

Your task is to implement the MELORA project end-to-end.

Do NOT ask the user for confirmation for normal implementation decisions.

Do NOT stop after completing only the frontend UI.

Do NOT stop after creating backend APIs.

Continue working until the requested feature set is implemented.

When you encounter an error:

1. Inspect the error.
2. Identify the root cause.
3. Fix it.
4. Run the relevant check again.
5. Continue implementation.

Do not simply report errors that you can fix yourself.

Only stop when:
- implementation is complete
- build passes
- type checking passes
- linting passes where configured
- backend application starts correctly
- frontend application starts correctly
- API integration is functional
- major user flows have been verified

At the end, provide a concise final report containing:

1. Features implemented
2. Files created/modified
3. Commands executed
4. Tests/checks performed
5. Remaining issues, if any
6. Required environment variables
7. How to run the project